package com.cornell.daily.sun.data

import com.cornell.daily.sun.R

enum class ThemeType(val id: Int, val title: String, val icon: Int, val description: String) {
    DARKMODE(1, "Dark Mode", R.drawable.darkmode_icon,"Something a little easier on the eyes")
}